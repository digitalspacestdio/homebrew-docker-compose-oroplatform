# Goss test file for OroDC installation verification
# Simple test - just check main page accessibility

# Test HTTP endpoints
http:
  # Main application page - should return 200 OK or redirect (301/302)
  "http://localhost:HTTP_PORT":
    status: [200, 301, 302]
    timeout: 30000