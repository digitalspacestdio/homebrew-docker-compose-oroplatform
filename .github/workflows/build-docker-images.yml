name: Build & Publish PHP Base and PHP+Node.js multi-arch images to GHCR

on:
  push:
    branches: [ master ]
    paths:
      - 'compose/docker/php/**'
      - 'compose/docker/php-node-symfony/**'
  workflow_dispatch:
    inputs:
      php_versions:
        description: 'PHP versions to build (comma-separated: 7.4,8.1,8.2,8.3,8.4,8.5 or "all")'
        required: false
        default: 'all'
      skip_tests:
        description: 'Skip test workflow trigger'
        type: boolean
        required: false
        default: false

permissions:
  contents: read
  packages: write

concurrency:
  group: docker-build-${{ github.job }}-${{ github.ref_name }}
  cancel-in-progress: true

jobs:
  # ========================================
  # STAGE 1: BASE PHP IMAGES
  # ========================================
  
  # PHP 7.4 (Legacy)
  build-php-7-4:
    name: Build PHP 7.4 Base (${{ matrix.arch_name }})
    runs-on:
      - self-hosted
      - Linux
      - ${{ matrix.arch }}
    strategy:
      fail-fast: false
      max-parallel: 2
      matrix:
        include:
          - arch: X64
            arch_name: amd64
            platform: linux/amd64
          - arch: ARM64
            arch_name: arm64
            platform: linux/arm64
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      # Login to Docker Hub (optional, increases rate limit from 100 to 200 pulls/6h)
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
        continue-on-error: true
      
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3
      
      # Build with retry (handles Docker Hub rate limits)
      - name: Build PHP 7.4 Base (${{ matrix.arch_name }})
        uses: nick-invision/retry@v2
        with:
          timeout_minutes: 30
          max_attempts: 3
          retry_wait_seconds: 60
          command: |
            docker buildx build \
              --file compose/docker/php/Dockerfile.7.4.alpine \
              --platform ${{ matrix.platform }} \
              --tag ghcr.io/${{ github.repository_owner }}/orodc-php:7.4-alpine-${{ matrix.arch_name }} \
              --build-arg PHP_VERSION=7.4 \
              --build-arg COMPOSER_VERSION=2 \
              --cache-from type=gha,scope=php-base-7.4-${{ matrix.arch }} \
              --cache-to type=gha,mode=max,scope=php-base-7.4-${{ matrix.arch }} \
              --load \
              compose/docker/php
      
      # Push with retry (handles network errors)
      - name: Push PHP 7.4 Base (${{ matrix.arch_name }})
        uses: nick-invision/retry@v2
        with:
          timeout_minutes: 10
          max_attempts: 3
          retry_wait_seconds: 30
          command: |
            docker push ghcr.io/${{ github.repository_owner }}/orodc-php:7.4-alpine-${{ matrix.arch_name }}

  # PHP 8.x (Stable: 8.1, 8.2, 8.3, 8.4)
  build-php-8-x:
    name: Build PHP ${{ matrix.php }} Base (${{ matrix.arch_config.arch_name }})
    runs-on:
      - self-hosted
      - Linux
      - ${{ matrix.arch_config.arch }}
    strategy:
      fail-fast: false
      max-parallel: 4
      matrix:
        php: ["8.1", "8.2", "8.3", "8.4"]
        arch_config:
          - arch: X64
            arch_name: amd64
            platform: linux/amd64
          - arch: ARM64
            arch_name: arm64
            platform: linux/arm64
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      # Login to Docker Hub (optional, increases rate limit from 100 to 200 pulls/6h)
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
        continue-on-error: true
      
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3
      
      # Build with retry (handles Docker Hub rate limits)
      - name: Build PHP ${{ matrix.php }} Base (${{ matrix.arch_config.arch_name }})
        uses: nick-invision/retry@v2
        with:
          timeout_minutes: 30
          max_attempts: 3
          retry_wait_seconds: 60
          command: |
            docker buildx build \
              --file compose/docker/php/Dockerfile.${{ matrix.php }}.alpine \
              --platform ${{ matrix.arch_config.platform }} \
              --tag ghcr.io/${{ github.repository_owner }}/orodc-php:${{ matrix.php }}-alpine-${{ matrix.arch_config.arch_name }} \
              --build-arg PHP_VERSION=${{ matrix.php }} \
              --build-arg COMPOSER_VERSION=2 \
              --cache-from type=gha,scope=php-base-${{ matrix.php }}-${{ matrix.arch_config.arch }} \
              --cache-to type=gha,mode=max,scope=php-base-${{ matrix.php }}-${{ matrix.arch_config.arch }} \
              --load \
              compose/docker/php
      
      # Push with retry (handles network errors)
      - name: Push PHP ${{ matrix.php }} Base (${{ matrix.arch_config.arch_name }})
        uses: nick-invision/retry@v2
        with:
          timeout_minutes: 10
          max_attempts: 3
          retry_wait_seconds: 30
          command: |
            docker push ghcr.io/${{ github.repository_owner }}/orodc-php:${{ matrix.php }}-alpine-${{ matrix.arch_config.arch_name }}

  # PHP 8.5-rc (RC/Beta)
  build-php-8-5:
    name: Build PHP 8.5-rc Base (${{ matrix.arch_name }})
    runs-on:
      - self-hosted
      - Linux
      - ${{ matrix.arch }}
    strategy:
      fail-fast: false
      max-parallel: 2
      matrix:
        include:
          - arch: X64
            arch_name: amd64
            platform: linux/amd64
          - arch: ARM64
            arch_name: arm64
            platform: linux/arm64
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      # Login to Docker Hub (optional, increases rate limit from 100 to 200 pulls/6h)
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
        continue-on-error: true
      
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3
      
      # Build with retry (handles Docker Hub rate limits)
      - name: Build PHP 8.5-rc Base (${{ matrix.arch_name }})
        uses: nick-invision/retry@v2
        with:
          timeout_minutes: 30
          max_attempts: 3
          retry_wait_seconds: 60
          command: |
            docker buildx build \
              --file compose/docker/php/Dockerfile.8.5.alpine \
              --platform ${{ matrix.platform }} \
              --tag ghcr.io/${{ github.repository_owner }}/orodc-php:8.5-rc-alpine-${{ matrix.arch_name }} \
              --build-arg PHP_VERSION=8.5-rc \
              --build-arg COMPOSER_VERSION=2 \
              --cache-from type=gha,scope=php-base-8.5-${{ matrix.arch }} \
              --cache-to type=gha,mode=max,scope=php-base-8.5-${{ matrix.arch }} \
              --load \
              compose/docker/php
      
      # Push with retry (handles network errors)
      - name: Push PHP 8.5-rc Base (${{ matrix.arch_name }})
        uses: nick-invision/retry@v2
        with:
          timeout_minutes: 10
          max_attempts: 3
          retry_wait_seconds: 30
          command: |
            docker push ghcr.io/${{ github.repository_owner }}/orodc-php:8.5-rc-alpine-${{ matrix.arch_name }}

  # ========================================
  # STAGE 2: CREATE MULTI-ARCH MANIFESTS
  # ========================================
  
  create-php-base-7-4-manifest:
    name: Create PHP 7.4 Base Multi-arch Manifest
    needs: [build-php-7-4]
    runs-on: [self-hosted, Linux, X64]
    steps:
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Create and push manifest
        run: |
          docker buildx imagetools create -t ghcr.io/${{ github.repository_owner }}/orodc-php:7.4-alpine \
            ghcr.io/${{ github.repository_owner }}/orodc-php:7.4-alpine-amd64 \
            ghcr.io/${{ github.repository_owner }}/orodc-php:7.4-alpine-arm64

  create-php-base-8-x-manifests:
    name: Create PHP ${{ matrix.php }} Base Multi-arch Manifest
    needs: [build-php-8-x]
    runs-on: [self-hosted, Linux, X64]
    strategy:
      matrix:
        php: ["8.1", "8.2", "8.3", "8.4"]
    steps:
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Create and push manifest for PHP ${{ matrix.php }}
        run: |
          docker buildx imagetools create -t ghcr.io/${{ github.repository_owner }}/orodc-php:${{ matrix.php }}-alpine \
            ghcr.io/${{ github.repository_owner }}/orodc-php:${{ matrix.php }}-alpine-amd64 \
            ghcr.io/${{ github.repository_owner }}/orodc-php:${{ matrix.php }}-alpine-arm64

  create-php-base-8-5-manifest:
    name: Create PHP 8.5-rc Base Multi-arch Manifest
    needs: [build-php-8-5]
    runs-on: [self-hosted, Linux, X64]
    steps:
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Create and push manifest
        run: |
          docker buildx imagetools create -t ghcr.io/${{ github.repository_owner }}/orodc-php:8.5-rc-alpine \
            ghcr.io/${{ github.repository_owner }}/orodc-php:8.5-rc-alpine-amd64 \
            ghcr.io/${{ github.repository_owner }}/orodc-php:8.5-rc-alpine-arm64

  # ========================================
  # STAGE 3: FINAL PHP+NODE.JS IMAGES
  # ========================================
  
  # PHP+Node.js 7.4 (Legacy)
  build-php-node-7-4:
    name: Build PHP+Node 7.4 (${{ matrix.arch_name }})
    needs: [create-php-base-7-4-manifest]
    runs-on:
      - self-hosted
      - Linux
      - ${{ matrix.arch }}
    strategy:
      fail-fast: false
      max-parallel: 2
      matrix:
        include:
          - arch: X64
            arch_name: amd64
            platform: linux/amd64
          - arch: ARM64
            arch_name: arm64
            platform: linux/arm64
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      # Login to Docker Hub
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
        continue-on-error: true
      
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3
      
      # Build with retry
      - name: Build PHP+Node 7.4 (${{ matrix.arch_name }})
        uses: nick-invision/retry@v2
        with:
          timeout_minutes: 30
          max_attempts: 3
          retry_wait_seconds: 60
          command: |
            docker buildx build \
              --file compose/docker/php-node-symfony/Dockerfile.7.4.alpine \
              --platform ${{ matrix.platform }} \
              --tag ghcr.io/${{ github.repository_owner }}/orodc-php-node-symfony:7.4-alpine-${{ matrix.arch_name }} \
              --cache-from type=gha,scope=php-node-7.4-${{ matrix.arch }} \
              --cache-to type=gha,mode=max,scope=php-node-7.4-${{ matrix.arch }} \
              --load \
              compose/docker/php-node-symfony
      
      # Push with retry
      - name: Push PHP+Node 7.4 (${{ matrix.arch_name }})
        uses: nick-invision/retry@v2
        with:
          timeout_minutes: 10
          max_attempts: 3
          retry_wait_seconds: 30
          command: |
            docker push ghcr.io/${{ github.repository_owner }}/orodc-php-node-symfony:7.4-alpine-${{ matrix.arch_name }}

  # PHP+Node.js 8.x (Stable: 8.1, 8.2, 8.3, 8.4)
  build-php-node-8-x:
    name: Build PHP+Node ${{ matrix.php }} (${{ matrix.arch_config.arch_name }})
    needs: [create-php-base-8-x-manifests]
    runs-on:
      - self-hosted
      - Linux
      - ${{ matrix.arch_config.arch }}
    strategy:
      fail-fast: false
      max-parallel: 4
      matrix:
        php: ["8.1", "8.2", "8.3", "8.4"]
        arch_config:
          - arch: X64
            arch_name: amd64
            platform: linux/amd64
          - arch: ARM64
            arch_name: arm64
            platform: linux/arm64
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      # Login to Docker Hub
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
        continue-on-error: true
      
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3
      
      # Build with retry
      - name: Build PHP+Node ${{ matrix.php }} (${{ matrix.arch_config.arch_name }})
        uses: nick-invision/retry@v2
        with:
          timeout_minutes: 30
          max_attempts: 3
          retry_wait_seconds: 60
          command: |
            docker buildx build \
              --file compose/docker/php-node-symfony/Dockerfile.${{ matrix.php }}.alpine \
              --platform ${{ matrix.arch_config.platform }} \
              --tag ghcr.io/${{ github.repository_owner }}/orodc-php-node-symfony:${{ matrix.php }}-alpine-${{ matrix.arch_config.arch_name }} \
              --cache-from type=gha,scope=php-node-${{ matrix.php }}-${{ matrix.arch_config.arch }} \
              --cache-to type=gha,mode=max,scope=php-node-${{ matrix.php }}-${{ matrix.arch_config.arch }} \
              --load \
              compose/docker/php-node-symfony
      
      # Push with retry
      - name: Push PHP+Node ${{ matrix.php }} (${{ matrix.arch_config.arch_name }})
        uses: nick-invision/retry@v2
        with:
          timeout_minutes: 10
          max_attempts: 3
          retry_wait_seconds: 30
          command: |
            docker push ghcr.io/${{ github.repository_owner }}/orodc-php-node-symfony:${{ matrix.php }}-alpine-${{ matrix.arch_config.arch_name }}

  # PHP+Node.js 8.5-rc (RC/Beta)
  build-php-node-8-5:
    name: Build PHP+Node 8.5-rc (${{ matrix.arch_name }})
    needs: [create-php-base-8-5-manifest]
    runs-on:
      - self-hosted
      - Linux
      - ${{ matrix.arch }}
    strategy:
      fail-fast: false
      max-parallel: 2
      matrix:
        include:
          - arch: X64
            arch_name: amd64
            platform: linux/amd64
          - arch: ARM64
            arch_name: arm64
            platform: linux/arm64
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      # Login to Docker Hub
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
        continue-on-error: true
      
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3
      
      # Build with retry
      - name: Build PHP+Node 8.5-rc (${{ matrix.arch_name }})
        uses: nick-invision/retry@v2
        with:
          timeout_minutes: 30
          max_attempts: 3
          retry_wait_seconds: 60
          command: |
            docker buildx build \
              --file compose/docker/php-node-symfony/Dockerfile.8.5.alpine \
              --platform ${{ matrix.platform }} \
              --tag ghcr.io/${{ github.repository_owner }}/orodc-php-node-symfony:8.5-alpine-${{ matrix.arch_name }} \
              --cache-from type=gha,scope=php-node-8.5-${{ matrix.arch }} \
              --cache-to type=gha,mode=max,scope=php-node-8.5-${{ matrix.arch }} \
              --load \
              compose/docker/php-node-symfony
      
      # Push with retry
      - name: Push PHP+Node 8.5-rc (${{ matrix.arch_name }})
        uses: nick-invision/retry@v2
        with:
          timeout_minutes: 10
          max_attempts: 3
          retry_wait_seconds: 30
          command: |
            docker push ghcr.io/${{ github.repository_owner }}/orodc-php-node-symfony:8.5-alpine-${{ matrix.arch_name }}

  # ========================================
  # STAGE 4: CREATE FINAL MULTI-ARCH MANIFESTS
  # ========================================
  
  create-php-node-7-4-manifest:
    name: Create PHP+Node 7.4 Multi-arch Manifest
    needs: [build-php-node-7-4]
    runs-on: [self-hosted, Linux, X64]
    steps:
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Create and push manifest
        run: |
          docker buildx imagetools create -t ghcr.io/${{ github.repository_owner }}/orodc-php-node-symfony:7.4-alpine \
            ghcr.io/${{ github.repository_owner }}/orodc-php-node-symfony:7.4-alpine-amd64 \
            ghcr.io/${{ github.repository_owner }}/orodc-php-node-symfony:7.4-alpine-arm64

  create-php-node-8-x-manifests:
    name: Create PHP+Node ${{ matrix.php }} Multi-arch Manifest
    needs: [build-php-node-8-x]
    runs-on: [self-hosted, Linux, X64]
    strategy:
      matrix:
        php: ["8.1", "8.2", "8.3", "8.4"]
    steps:
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Create and push manifest for PHP+Node ${{ matrix.php }}
        run: |
          docker buildx imagetools create -t ghcr.io/${{ github.repository_owner }}/orodc-php-node-symfony:${{ matrix.php }}-alpine \
            ghcr.io/${{ github.repository_owner }}/orodc-php-node-symfony:${{ matrix.php }}-alpine-amd64 \
            ghcr.io/${{ github.repository_owner }}/orodc-php-node-symfony:${{ matrix.php }}-alpine-arm64

  create-php-node-8-5-manifest:
    name: Create PHP+Node 8.5-rc Multi-arch Manifest
    needs: [build-php-node-8-5]
    runs-on: [self-hosted, Linux, X64]
    steps:
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Create and push manifest
        run: |
          docker buildx imagetools create -t ghcr.io/${{ github.repository_owner }}/orodc-php-node-symfony:8.5-alpine \
            ghcr.io/${{ github.repository_owner }}/orodc-php-node-symfony:8.5-alpine-amd64 \
            ghcr.io/${{ github.repository_owner }}/orodc-php-node-symfony:8.5-alpine-arm64
